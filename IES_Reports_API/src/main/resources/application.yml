spring:
  application:
    name: IES_Reports_API

  datasource:
    username: root
    password: Root@0402
    url: jdbc:mysql://localhost:3306/project
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


logging:
   level:
      root: info